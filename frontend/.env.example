# Set API base to connect directly to backend (bypassing Vite proxy)
VITE_API_BASE=http://localhost:8000

# Your Path of Exile account name (used to highlight your own trades)
VITE_ACCOUNT_NAME=YourAccountName#1234

# ============================================================================
# Polling Intervals (in milliseconds)
# ============================================================================

# How often to check for expiring cache entries (default: 120000 = 2 minutes)
# Lower values = more responsive but more API calls
VITE_CACHE_CHECK_INTERVAL=120000

# How often to force refresh all pairs as a background sync (default: 900000 = 15 minutes)
# This ensures all data eventually syncs even if smart refresh misses something
VITE_GLOBAL_REFRESH_INTERVAL=900000

# How often to take portfolio snapshots (default: 900000 = 15 minutes)
VITE_PORTFOLIO_SNAPSHOT_INTERVAL=900000